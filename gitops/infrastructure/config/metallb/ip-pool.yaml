# MetalLB L2 address pool. CONFIRM 10.0.5.80-99 is free on your LAN before use.
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: default-pool
  namespace: metallb-system
spec:
  addresses:
    - 10.0.5.80-10.0.5.99
  autoAssign: true
